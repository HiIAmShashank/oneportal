/**
 * @module @one-portal/auth/initialization
 * @description MSAL initialization utilities for host and remote applications
 */

export { MsalInitializer } from './MsalInitializer';
export type {
    InitConfig,
    InitializationMode,
    InitializationState,
    InitializationResult,
    InitializationCallback,
    RouteType,
} from './types';
