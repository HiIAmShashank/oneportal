/**
 * OnePortal Backend API
 *
 * Barrel export for clean imports throughout the Shell application.
 *
 * @example
 * ```typescript
 * import { checkSuperUser, createUser, type CreateUserDto } from '../api';
 * import { API_ENDPOINTS } from '../api';
 * ```
 */

export * from "./types";
export * from "./client";
export * from "./constants";
