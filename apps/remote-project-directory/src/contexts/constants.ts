// Constants moved to a separate file or inside the component to satisfy Fast Refresh
const BATCH_SIZE = 20;
const DEBOUNCE_MS = 200;
const MAX_CONCURRENT_BATCHES = 3;

export { BATCH_SIZE, DEBOUNCE_MS, MAX_CONCURRENT_BATCHES };
